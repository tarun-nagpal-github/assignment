# Copy to .env and set a strong admin password (required for OpenSearch 2.12+)
# Minimum 8 characters; use high entropy (avoid dictionary words, sequences).
# Example: OPENSEARCH_INITIAL_ADMIN_PASSWORD=MyStr0ng!P@ssw0rd

OPENSEARCH_INITIAL_ADMIN_PASSWORD=ChangeMeStrongPassword123!
